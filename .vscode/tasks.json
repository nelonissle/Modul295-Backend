{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Tests and Build",
            "type": "shell",
            "command": "dotnet test; if ($?) { dotnet clean; dotnet build }",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
